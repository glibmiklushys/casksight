{
  "name": "casksight",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Watch a Bitcask-style key/value log: append, index, tombstones, and compaction.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vite": "^6.2.2",
    "vitest": "^3.0.9"
  }
}
